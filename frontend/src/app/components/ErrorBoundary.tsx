/**
 * Backwards-compatible re-export. New code should import `GlobalErrorBoundary`
 * from `./GlobalErrorBoundary` (also used as the root boundary in `App.tsx`).
 */
export { GlobalErrorBoundary, default as ErrorBoundary } from './GlobalErrorBoundary';
